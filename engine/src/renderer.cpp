#include "renderer.hpp"
#include <library/opengl/opengl.hpp>
using namespace library;

/*bool Renderer::render(WindowClass& wnd, double dtime, double timeElapsed)
{
	float cl = 0.5 + 0.5 * std::sin(2 * timeElapsed);

	glClearColor(cl, cl, cl, 1.0);
	glClear(GL_COLOR_BUFFER_BIT);



	glfwSwapBuffers(wnd.window());

	return true;
}*/
